#!/usr/bin/python
#可以使用内建的dir函数来列出模块定义的标识符，标识符包括函数，类，变量。
#当为dir()提供一个模块名时，会返回模块定义的名称列表；若不提供参数，则返回当前模块中定义的名称列表。



#Filename: dir.py
import sys;

dir(sys);#get list of attributes for sys module
